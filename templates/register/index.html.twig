{% extends 'base.html.twig' %} {% block title %}Inscription - Good Morning{%
endblock %} {% block content %}
<h1 style="text-align: center" class="mt-3">Inscription</h1>
<div class="row mt-4 justify-content-center"  >
<div  class="col-10 ">

   {% if notification %}
     <div class="alert alert-info" >{{notification}}</div>
     <hr>
     
   {% endif %}
  {{ form(form) }}
</div>
</div>
{% endblock %}
